# Golang CircleCI 2.0 configuration file
version: 2
jobs:
  build:
    docker:
      - image: circleci/golang:1.13
    working_directory: /go/src/rjjatson.com/bacot-auth-service
    steps:
      - checkout
      - run: go build ./main.go
      - run: go test -v ./...